{
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "test": "jest",
    "lint": "lerna run --stream lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.7",
    "jest": "^24.7.1",
    "lerna": "^3.13.4",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
